#include<stdio.h>
int main(){
    float a,b,force;
    printf("the mass is:");
    scanf("%f",&a);
    printf("acceleration is :");
    scanf("%f",&b);
    force=a*b;
    printf("the force is %f\n",force);

}